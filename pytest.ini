[pytest]
# Put the repo root on sys.path so tests can do `from common.pitch_utils import ...`
# without an editable install. Bare `pytest` (what CI runs) does not add the
# working directory to sys.path the way `python -m pytest` does.
pythonpath = .
testpaths = tests
